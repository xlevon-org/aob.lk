---
description: Break an approved design into a numbered checklist, each task individually verifiable.
argument-hint: "[feature-slug] (defaults to the current spec)"
---

Invoke the `tasks` skill for: `$ARGUMENTS`

Order innermost-first, and give every task one command that proves it.
