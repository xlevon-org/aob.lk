---
description: Run the full verification across every workspace and report the real counts.
argument-hint: "[scoped <paths> | full] (default: full)"
---

Invoke the `regression` skill for: `$ARGUMENTS`

Paste the counts. A red gate blocks everything downstream.
