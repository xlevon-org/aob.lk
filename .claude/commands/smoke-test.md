---
description: Fast health check - build, serve, confirm the key pages render.
argument-hint: "(none)"
---

Invoke the `smoke-test` skill for: `$ARGUMENTS`

Paste the build output, the HTTP status and the per-page results.
