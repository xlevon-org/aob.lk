---
description: Produce the technical design - layer placement, signatures, data flow, contracts.
argument-hint: "[feature-slug] (defaults to the current spec)"
---

Invoke the `design` skill for: `$ARGUMENTS`

Justify every layer placement against the dependency rule, and put anything crossing app boundaries in `packages/shared`.
