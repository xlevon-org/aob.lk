---
description: Write tests on the Node runner - happy, boundary and unhappy, proven to fail on regression.
argument-hint: "<module or behaviour to test>"
---

Invoke the `unit-tests` skill for: `$ARGUMENTS`

Enumerate the matrix rather than picking examples, then break the implementation deliberately to prove the tests bite.
