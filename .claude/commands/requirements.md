---
description: Turn a request into requirements.md - assumptions, scenario matrix, EARS acceptance criteria.
argument-hint: "<the request to analyse>"
---

Invoke the `requirements` skill for: `$ARGUMENTS`

Enumerate the combinations rather than the happy path, and make every acceptance criterion name how it will be proved.
