---
description: Final GO / NO-GO gate against every acceptance criterion, with named evidence.
argument-hint: "[feature-slug] (defaults to the current spec)"
---

Invoke the `evaluate` skill for: `$ARGUMENTS`

Every PASS names a concrete artifact. A hedge is a NO-GO.
