---
description: Build the scenario matrix for a change, or audit an existing suite by mutation.
argument-hint: "[scope <feature> | evaluate <suite/path>]"
---

Invoke the `test-plan` skill for: `$ARGUMENTS`

A row with no coverage needs a written waiver, not silence.
