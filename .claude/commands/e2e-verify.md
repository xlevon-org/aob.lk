---
description: Prove a behaviour in a real browser with measured values, not screenshots alone.
argument-hint: "<behaviour or scenario to verify>"
---

Invoke the `e2e-verify` skill for: `$ARGUMENTS`

Name the expected value before measuring, and verify the built bundle rather than the dev server.
