---
description: Verify layout across the full width range - overflow, breakpoints, reachability.
argument-hint: "<page, component, or 'all'>"
---

Invoke the `responsive` skill for: `$ARGUMENTS`

Measure rather than look, and check the exact breakpoint boundary rather than near it.
