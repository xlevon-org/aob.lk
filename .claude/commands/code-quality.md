---
description: Review the current diff against this codebase's real failure modes.
argument-hint: "[diff range, e.g. master...HEAD] (default: working tree)"
---

Invoke the `code-quality` skill for: `$ARGUMENTS`

Read the actual diff, not your memory of the intent. Findings most-severe first with `file:line`.
